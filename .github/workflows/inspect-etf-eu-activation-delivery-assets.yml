name: Historical ETF EU activation asset inspection (archived)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - name: Historical workflow notice
        run: |
          echo "Archived one-off activation and delivery-asset inspection."
          echo "It has no current PR-gate, release, transport, or receipt authority."
          echo "Use the canonical release-assurance and governed routine workflows."
          echo "The original implementation remains available in Git history."
