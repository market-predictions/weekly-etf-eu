name: Legacy ETF EU July routine preview — quarantined

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  quarantine-notice:
    runs-on: ubuntu-latest
    steps:
      - name: Explain replacement
        run: |
          echo "ETF_EU_LEGACY_JULY_ROUTINE_PREVIEW_QUARANTINED"
          echo "This workflow is pinned to report date 2026-07-31, an earlier donor snapshot, and the pre-activation three-position allocator contract."
          echo "It is not authoritative for the current four-position, partially activated model state."
          echo "Use the state-driven production contract and fresh production package workflows."
