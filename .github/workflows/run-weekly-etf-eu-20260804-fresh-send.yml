name: Build fresh governed Weekly ETF EU candidate

on:
  pull_request:
    paths:
      - ".github/workflows/run-weekly-etf-eu-20260804-fresh-send.yml"
      - "config/etf_eu_portfolio_policy_v2.yml"
      - "tools/run_etf_eu_aug3_expanded_report_v6.py"
      - "tools/build_etf_eu_converged_routine_package.py"
      - "tools/validate_etf_eu_converged_routine_manifest.py"
      - "tools/validate_etf_eu_portfolio_policy.py"
      - "tests/test_validate_etf_eu_portfolio_policy.py"
      - "tools/validate_etf_eu_production_converged_report.py"
      - "tests/test_etf_eu_production_converged_report_modes.py"
      - "pricing/build_current_session_close_results.py"
      - "pricing/build_current_session_close_results_v2.py"
      - "runtime/build_etf_eu_routine_valuation_overlay.py"
      - "runtime/build_etf_eu_production_convergence_state_v2.py"
      - "runtime/reconcile_etf_eu_stage1_current_evidence.py"
      - "runtime/apply_etf_eu_routine_valuation_to_client_report_v2.py"
      - "runtime/add_etf_eu_activated_allocation_surface.py"
      - "runtime/add_etf_eu_activated_allocation_surface_v2.py"
      - "tools/validate_etf_eu_production_convergence_state.py"
      - "tools/build_etf_eu_release_assurance.py"
      - "tools/validate_etf_eu_release_assurance.py"
      - "output/etf_eu_portfolio_state.json"
      - "output/etf_eu_trade_ledger.csv"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-fresh-package:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    env:
      REPORT_DATE: "2026-08-05"
      REPORT_SUFFIX: "260805_02"
      DONOR_COMMIT: "f61e59e6b4b8f7fc02a16eb42f01f23d8b906985"
      RUN_ID: "20260805_${{ github.run_id }}_${{ github.run_attempt }}"
      SOURCE_SHA: "${{ github.event.pull_request.head.sha || github.sha }}"
      TARGET_BRANCH: "${{ github.head_ref || github.ref_name }}"
      PYTHONPATH: "${{ github.workspace }}"

    steps:
      - name: Checkout Weekly ETF EU candidate branch
        uses: actions/checkout@v4
        with:
          ref: ${{ env.TARGET_BRANCH }}
          fetch-depth: 0

      - name: Checkout Weekly ETF donor
        uses: actions/checkout@v4
        with:
          repository: market-predictions/weekly-etf
          ref: ${{ env.DONOR_COMMIT }}
          path: donor-weekly-etf

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install bounded production dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements.txt
          python -m pip install yfinance weasyprint pypdf beautifulsoup4 pyyaml matplotlib requests pytest

      - name: Validate state and policy contracts
        run: |
          python -m py_compile \
            tools/validate_etf_eu_production_converged_report.py \
            tools/validate_etf_eu_portfolio_policy.py \
            tools/build_etf_eu_converged_routine_package.py \
            tools/validate_etf_eu_converged_routine_manifest.py \
            tools/build_etf_eu_release_assurance.py \
            tools/validate_etf_eu_release_assurance.py
          python -m pytest -q \
            tests/test_etf_eu_production_converged_report_modes.py \
            tests/test_validate_etf_eu_portfolio_policy.py

      - name: Build and validate policy-bound package
        id: build_package
        shell: bash
        run: |
          mkdir -p output/diagnostics
          set +e
          set -o pipefail
          python tools/run_etf_eu_aug3_expanded_report_v6.py \
            --donor-dir donor-weekly-etf \
            --report-date "$REPORT_DATE" \
            --report-suffix "$REPORT_SUFFIX" \
            --run-id "$RUN_ID" \
            --source-sha "$SOURCE_SHA" \
            --donor-commit "$DONOR_COMMIT" \
            2>&1 | tee "output/diagnostics/fresh_package_build_${RUN_ID}.log"
          status=${PIPESTATUS[0]}
          {
            echo "build_status=$status"
            echo "run_id=$RUN_ID"
            echo "report_date=$REPORT_DATE"
            echo "report_suffix=$REPORT_SUFFIX"
            echo "source_sha=$SOURCE_SHA"
            echo "target_branch=$TARGET_BRANCH"
            echo "generated_files:"
            find output -maxdepth 4 -type f -printf '%p\n' | sort
          } > "output/diagnostics/fresh_package_inventory_${RUN_ID}.txt"
          exit "$status"

      - name: Upload fresh-package diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: etf-eu-fresh-package-diagnostics-${{ env.RUN_ID }}
          path: output/diagnostics/
          if-no-files-found: warn
          retention-days: 30

      - name: Validate final lineage, portfolio and report identity
        run: |
          python - <<'PY'
          import hashlib, json, os
          from pathlib import Path

          def sha256(path: Path) -> str:
              return hashlib.sha256(path.read_bytes()).hexdigest()

          run_id = os.environ['RUN_ID']
          report_date = os.environ['REPORT_DATE']
          state_path = Path(f'output/routine_preview/etf_eu_production_convergence_state_{run_id}.json')
          policy_path = Path(f'output/routine_preview/etf_eu_portfolio_policy_validation_{run_id}.json')
          routine_path = Path(f'output/run_manifests/etf_eu_routine_run_manifest_{report_date}_{run_id}.json')
          pricing_path = Path(f'output/pricing/ucits_price_provider_qualification_{run_id}.json')
          protected_path = Path('output/etf_eu_portfolio_state.json')
          state = json.loads(state_path.read_text())
          policy = json.loads(policy_path.read_text())
          routine = json.loads(routine_path.read_text())
          pricing = json.loads(pricing_path.read_text())
          protected = json.loads(protected_path.read_text())
          portfolio = state['official_portfolio']
          tickers = {str(row.get('ticker') or row.get('exchange_ticker')) for row in portfolio['positions']}
          expected = {str(row.get('ticker') or row.get('exchange_ticker')) for row in protected['positions']}
          if tickers != expected:
              raise SystemExit(f'FINAL_FUNDED_TICKER_LINEAGE_MISMATCH:{sorted(tickers)}:{sorted(expected)}')
          if policy.get('verdict') != 'PASS' or policy.get('blockers'):
              raise SystemExit(f'FINAL_PORTFOLIO_POLICY_FAILED:{policy.get("blockers")}')
          if policy.get('allocation_lineage', {}).get('share_or_cash_mutation_detected') is not False:
              raise SystemExit('FINAL_UNAUTHORIZED_SHARE_OR_CASH_MUTATION')
          if policy.get('authoritative_state_sha256') != sha256(protected_path):
              raise SystemExit('FINAL_PROTECTED_STATE_HASH_MISMATCH')
          if portfolio.get('position_count') != len(expected):
              raise SystemExit('FINAL_POSITION_COUNT_MISMATCH')
          if pricing.get('funded_consensus_count') != len(expected) or pricing.get('funded_identity_anchor_count') != len(expected):
              raise SystemExit('FINAL_FUNDED_PRICING_GATE_NOT_COMPLETE')
          if pricing.get('report_pricing_gate_passed') is not True:
              raise SystemExit('FINAL_REPORT_PRICING_GATE_FAILED')
          stage = state.get('stage_1_decision') or {}
          if set(stage.get('activated_tickers') or []) != {'L0CK'}:
              raise SystemExit('FINAL_ACTIVATED_TICKER_SCOPE_MISMATCH')
          if set(stage.get('remaining_monitored_tickers') or []) != {'VVSM'}:
              raise SystemExit('FINAL_MONITORED_TICKER_SCOPE_MISMATCH')
          if routine.get('report_suffix') != os.environ['REPORT_SUFFIX']:
              raise SystemExit('FINAL_REPORT_SUFFIX_MISMATCH')
          if routine.get('portfolio_snapshot', {}).get('portfolio_policy_verdict') != 'PASS':
              raise SystemExit('FINAL_MANIFEST_POLICY_NOT_PASS')
          if routine.get('delivery_authority') is not False:
              raise SystemExit('FINAL_REPORT_DELIVERY_AUTHORITY_ESCALATED')
          print(json.dumps({
              'status':'validated',
              'run_id':run_id,
              'report_date':report_date,
              'report_suffix':routine.get('report_suffix'),
              'policy_id':policy.get('policy_id'),
              'allocation_lineage':policy.get('allocation_lineage'),
              'funded_tickers':sorted(tickers),
              'computed_weights_pct':policy.get('portfolio', {}).get('computed_weights_pct'),
              'largest_position_ticker':policy.get('portfolio', {}).get('largest_position_ticker'),
              'largest_position_weight_pct':policy.get('portfolio', {}).get('largest_position_weight_pct'),
              'cash_weight_pct':policy.get('portfolio', {}).get('cash_weight_pct'),
              'funded_consensus_count':pricing.get('funded_consensus_count'),
              'position_count':portfolio.get('position_count'),
              'nav_eur':portfolio.get('nav_eur'),
              'cash_eur':portfolio.get('cash_eur'),
              'activated_tickers':stage.get('activated_tickers'),
              'remaining_monitored_tickers':stage.get('remaining_monitored_tickers'),
              'real_broker_execution':portfolio.get('real_broker_execution'),
          },indent=2,sort_keys=True))
          PY

      - name: Persist exact final package and policy evidence to candidate branch
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -f \
            output/fresh_generation/weekly_etf_eu_review_nl_${REPORT_SUFFIX}.html \
            output/fresh_generation/weekly_etf_eu_review_nl_${REPORT_SUFFIX}.pdf \
            output/fresh_generation/weekly_etf_eu_review_${REPORT_SUFFIX}.html \
            output/fresh_generation/weekly_etf_eu_review_${REPORT_SUFFIX}.pdf \
            output/run_manifests/etf_eu_routine_run_manifest_${REPORT_DATE}_${RUN_ID}.json \
            output/pricing/ucits_close_price_validation_basket_results_${RUN_ID}.json \
            output/pricing/ucits_price_provider_qualification_${RUN_ID}.json \
            output/macro/etf_eu_current_macro_source_${RUN_ID}.json \
            output/macro/etf_eu_macro_policy_${RUN_ID}.json \
            output/routine_preview/etf_eu_production_convergence_state_${RUN_ID}.json \
            output/routine_preview/etf_eu_portfolio_policy_validation_${RUN_ID}.json \
            output/routine_preview/etf_eu_production_converged_report_validation_${RUN_ID}.json \
            output/routine_preview/etf_eu_converged_routine_manifest_validation_${RUN_ID}.json \
            output/routine_preview/etf_eu_aug3_expanded_report_run_summary_${RUN_ID}.json
          if git diff --cached --quiet; then
            echo "No fresh package changes to persist."
            exit 0
          fi
          git commit -m "Persist lineage-bound Weekly ETF EU ${REPORT_SUFFIX} candidate [skip ci]"
          git reset --hard HEAD
          git clean -fdx
          git fetch origin "$TARGET_BRANCH"
          git rebase "origin/$TARGET_BRANCH"
          git push origin HEAD:"$TARGET_BRANCH"

      - name: Upload final package evidence
        uses: actions/upload-artifact@v4
        with:
          name: etf-eu-final-package-${{ env.RUN_ID }}
          path: |
            output/fresh_generation/weekly_etf_eu_review*${{ env.REPORT_SUFFIX }}*
            output/run_manifests/etf_eu_routine_run_manifest_${{ env.REPORT_DATE }}_${{ env.RUN_ID }}.json
            output/pricing/ucits_close_price_validation_basket_results_${{ env.RUN_ID }}.json
            output/pricing/ucits_price_provider_qualification_${{ env.RUN_ID }}.json
            output/macro/etf_eu_current_macro_source_${{ env.RUN_ID }}.json
            output/macro/etf_eu_macro_policy_${{ env.RUN_ID }}.json
            output/routine_preview/etf_eu_production_convergence_state_${{ env.RUN_ID }}.json
            output/routine_preview/etf_eu_portfolio_policy_validation_${{ env.RUN_ID }}.json
            output/routine_preview/etf_eu_production_converged_report_validation_${{ env.RUN_ID }}.json
            output/routine_preview/etf_eu_converged_routine_manifest_validation_${{ env.RUN_ID }}.json
            output/routine_preview/pdf_pages_${{ env.RUN_ID }}/
          if-no-files-found: error
          retention-days: 90
