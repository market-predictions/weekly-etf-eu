name: Disabled inherited US ETF send workflow

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Explain disabled workflow
        run: |
          echo "DISABLED_INHERITED_US_ETF_SEND_WORKFLOW"
          echo "This repository is market-predictions/weekly-etf-eu."
          echo "The inherited U.S.-ETF production send workflow is intentionally disabled."
          echo "Use .github/workflows/send-weekly-etf-eu-report.yml for EU/UCITS bootstrap validation."
          echo "No pricing, portfolio mutation, PDF rendering or email delivery is performed by this workflow."
