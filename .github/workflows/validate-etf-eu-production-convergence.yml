name: Historical ETF EU July production convergence (archived)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - name: Historical workflow notice
        run: |
          echo "Archived July 2026 production-convergence experiment."
          echo "Its allocator assumptions predate the authoritative Stage-1 activation."
          echo "It has no current PR-gate or production-release authority."
          echo "Use the state-derived validator and governed fresh-package workflow."
          echo "The original implementation remains available in Git history."
