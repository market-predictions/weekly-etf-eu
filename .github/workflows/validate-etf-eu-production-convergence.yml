name: Legacy ETF EU blocked-state convergence — quarantined

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  quarantine-notice:
    runs-on: ubuntu-latest
    steps:
      - name: Explain replacement
        run: |
          echo "ETF_EU_LEGACY_BLOCKED_STATE_CONVERGENCE_QUARANTINED"
          echo "This WP10 workflow is pinned to report date 2026-07-29, the three-position legacy set, and a blocked Stage-1 allocator contract."
          echo "It is not authoritative for the current partially activated four-position model state."
          echo "Current assurance is provided by validate-etf-eu-production-contract.yml, validate-etf-eu-release-assurance.yml, and the fresh production package workflow."
