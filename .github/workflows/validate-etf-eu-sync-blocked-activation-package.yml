name: Historical ETF EU blocked-activation validation (archived)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - name: Historical workflow notice
        run: |
          echo "Archived blocked-state validation from the pre-activation migration."
          echo "It is incompatible with the authoritative partially activated four-position state."
          echo "It has no PR-gate, portfolio, report, or delivery authority."
          echo "The original implementation remains available in Git history."
