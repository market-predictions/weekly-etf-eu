# Weekly ETF EU Review OS — Next Actions

## Current priority

```text
FINISH_ONE_FRESH_GOVERNED_CLIENT_CANDIDATE_THEN_RUN_INDEPENDENT_RELEASE_ASSURANCE
```

The highest-priority replay-safe pricing work package has reached a machine-validated result. The project should now use the repaired product boundary, allocation lineage and pricing lineage to produce one reviewable Dutch/English Weekly ETF EU candidate. Do not jump directly to email.

## Completed / materially resolved

### A. Product-boundary repair

```text
status=PASS_ON_REMEDIATION_BRANCH
```

- Active FX scheduler/generator assets are removed from the ETF EU remediation branch.
- Product-boundary CI is green.
- Weekly FX output must not be generated by this repository.

### B. Allocation-lineage repair

```text
policy_id=ETF_EU_RELEASE_LINEAGE_POLICY_V2
status=PASS_ARCHITECTURE
```

- Valuation-only runs preserve protected tickers, shares and cash.
- L0CK is bound to the explicit Stage-1 allocation decision and activation ID.
- Share/cash mutation without explicit authority fails closed.
- NAV, market values, shares × price and weights must reconcile.
- No unsupported universal 50% position cap exists.
- No unsupported mandatory 35% cash floor exists.
- Concentration remains an underwriting observation unless a separate decision creates a hard cap.

### C. Replay-safe completed-close lineage

```text
work_package=ETF-EU-WP-SYNC-11B
status=RELEASE_CANDIDATE_READY
implementation_ci=PASS
workflow_run_id=31254153417
job_id=93094895139
```

- The 2026-08-05 report-time two-provider evidence is preserved immutably.
- Exact report date, basket, ticker, ISIN, MIC, currency, provider set and provider symbol are enforced.
- Source workflow SHA, Actions artifact ID/digest and qualification-file SHA-256 are bound.
- 4/4 funded two-provider replay passes.
- 4/4 funded identity-anchor replay passes.
- Later mutable provider history cannot silently rewrite an already accepted report-time close.

## Active workstream — fresh client candidate

```text
owner_role=implementation_operations
status=IN_PROGRESS
```

Required sequence:

1. Complete the currently running fresh governed candidate build.
2. Require funded completed-close pricing PASS for every protected holding.
3. Preserve exact protected shares/cash unless a separately authorized allocation decision exists.
4. Build exactly four client files:
   - Dutch HTML;
   - Dutch PDF;
   - English HTML;
   - English PDF.
5. Bind the package to one immutable run ID, report date, source SHA, donor SHA, protected-state hash, allocation-decision hash, pricing-evidence hash and policy hash.
6. Run machine report validation.
7. Run rendered-page visual review.
8. Persist the exact candidate without delivery authority.

## Next workstream — independent release assurance

```text
owner_role=governance_release_assurance
status=NOT_YET_RUN_ON_FINAL_CANDIDATE
```

The assurance role must independently verify:

- product identity is Weekly ETF EU;
- no FX artifacts or execution paths are part of the candidate;
- protected portfolio lineage is preserved;
- pricing evidence is current or replay-safe and exact-date bound;
- report/state/pricing values agree;
- Dutch/English factual parity holds;
- four client artifacts and their hashes are complete;
- visual review passes;
- implementation did not self-certify.

Only then may the status become `GOVERNANCE_PASS_PRE_SEND`.

## User review boundary

Prompt the user to review only when:

```text
product_boundary_validation=PASS
allocation_lineage_validation=PASS
funded_pricing_gate=PASS
four_file_package_complete=true
machine_report_validation=PASS
visual_review=PASS
independent_release_assurance=PASS
email_delivery=false
```

The user review is of the exact immutable candidate that would later be sent.

## Delivery after review — not yet authorized

After explicit user approval only:

1. create the delivery queue bound to exact candidate and assurance hashes;
2. execute guarded ETF EU transport;
3. verify transport evidence;
4. independently verify inbox receipt and attachment identity;
5. write production closeout evidence;
6. claim `OUTCOME_CONFIRMED` only when receipt evidence exists.

## Remaining maturation backlog after the next successful candidate

1. Create one immutable run-input manifest covering donor, protected state, ledger, UCITS identity, pricing and macro evidence.
2. Reduce workflow sprawl to a production allowlist:
   - one generation workflow;
   - one assurance workflow;
   - one guarded transport workflow;
   - one receipt-closeout workflow;
   - bounded diagnostics without delivery authority.
3. Archive or disable historical send/generation workflows so manual invocation cannot bypass current governance.
4. Designate one canonical production renderer and retire alternative production paths.
5. Reconcile the EU decision framework with the mature Weekly ETF capital re-underwriting discipline without importing U.S. instrument/state authority.
6. Complete one governed post-review delivery with independent receipt confirmation.

## Prohibited shortcuts

Do not:

- run or surface FX output from the ETF EU repository;
- interpret the donor's 75% pricing-coverage threshold as a position-weight rule;
- invent a 50% cap or cash floor without an explicit decision record;
- let donor opportunity state silently mutate the EU portfolio;
- use later adjusted historical data to silently rewrite an accepted report-time close;
- weaken the two-provider pricing gate because a live historical provider is unavailable;
- treat arithmetic consistency as allocation authority;
- treat a rendered report as a completed release;
- build a delivery queue before user review;
- let implementation self-certify governance PASS;
- claim delivery from SMTP success alone.
